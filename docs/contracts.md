# Contracts

The Soroban workspace contains four packages:

- `institution-registry`
- `credential-registry`
- `reputation`
- `verification`

Each package currently exposes a minimal contract entry point so the monorepo has a concrete Soroban workspace structure ready for implementation and testing.
